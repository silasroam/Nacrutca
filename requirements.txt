# -- Code generated by Cline --
# Traffic Bot

python-telegram-bot==21.11.1
SQLAlchemy==2.0.32
aiosqlite==0.20.0
environ==1.0
python-dotenv==1.0.1
rich==13.7.1

# httpx is a direct runtime dependency (bot/services/blockchain.py, crypto_rates.py).
# It is imported at module load time by the API entry point, so it MUST be pinned
# explicitly even though python-telegram-bot also depends on it transitively.
httpx==0.27.2